# 插件改造测试指南（v2 更新）

## 改造内容（第二轮）

本次改造基于用户测试反馈，修复了以下问题：

1. **页面检测误报修复**：按钮现在严格限制在 `/page/unify/asset-overview` 路由下显示，防止在其他页面误显
2. **商品分析下载流程**：商品分析模式下，自动点击"内容消费"tab 后再下载，确保数据正确
3. **下载超时扩展**：超时时间从 20 秒扩展到 2 分钟，适应大文件下载
4. **作品分析商品链接**：作品分析面板新增商品ID列，点击可跳转到天猫商品详情页

---

## 本地测试步骤

### 1. 在 Chrome 中加载/重新加载插件

打开 Chrome → `chrome://extensions` → 启用「开发者模式」(右上角) 
- 如已加载：点击插件卡片上的「刷新」按钮（圆形箭头图标）
- 如未加载：「加载已解压的扩展程序」→ 选择 `/Users/xinjiabo/子城的AI助手/子城的诊断工具/淘宝短视频插件` 文件夹

### 2. 修改代码后重新加载

编辑任何 `.js` 文件后，在 Chrome 扩展页面中点击插件卡片上的「刷新」按钮

---

## 测试场景

### 场景 1：首页不显示按钮（修复误报）

1. 访问 https://creator.guanghe.taobao.com/page/unify/asset-overview
2. **预期**：右侧没有竖排的分析按钮（修复前会误显）
3. **实际**：_____

### 场景 2：作品分析页正常工作

1. 访问 https://creator.guanghe.taobao.com/page/unify/asset-overview?tab=singleEffect
2. **预期**：右侧出现"素材分析"竖排按钮（橙色）
3. **实际**：_____
4. 点击按钮 → 自动触发下载，或点击"上传 Excel"上传 `/Users/xinjiabo/Downloads/光合_资产总览_作品分析_*.xlsx`
5. **预期**：面板显示，第二列标题为"内容名称"，有"发布时间"列，**有新增的"商品ID"列**，指标正常显示
6. **实际**：_____
7. 检验商品ID列：点击商品ID中的链接，应跳转到天猫商品详情页面
8. **预期**：天猫商品详情页正常打开（如 https://detail.tmall.com/item.htm?id=588481316312）
9. **实际**：_____

### 场景 3：商品分析页正常工作（包含前置动作）

1. 访问 https://creator.guanghe.taobao.com/page/unify/asset-overview?tab=productAnalysis
2. **预期**：右侧出现"商品分析"竖排按钮（橙色）
3. **实际**：_____
4. 点击按钮 → 自动点击"内容消费"tab，然后触发下载
5. **预期**：进度条显示"切换到内容消费..."，稍后展开指标并点击下载
6. **实际**：_____
7. 点击按钮 → 自动触发下载，或点击"上传 Excel"上传商品分析 Excel
8. **预期**：面板显示，面板标题为"商品分析"，第二列标题为"商品ID"，不显示"发布时间"列，指标正常显示
9. **实际**：_____

### 场景 4：大文件下载超时扩展

1. 在作品分析或商品分析页面，点击"重新获取"按钮触发自动下载
2. 选择一个较大的 Excel 文件（或网络较慢的情况）
3. **预期**：等待进度条达到第4步"等待文件下载..."，即使超过 20 秒也不会提示超时（现在超时时间是 2 分钟）
4. **实际**：_____

### 场景 5：两页面间切换

1. 从作品分析页切换到商品分析页（点击导航中的"商品分析"tab 或手动修改 URL）
2. **预期**：按钮从"素材分析"变为"商品分析"，旧数据清空，可重新下载
3. **实际**：_____
4. 从商品分析页切换回作品分析页
5. **预期**：按钮文本变回"素材分析"，作品分析的旧数据仍保留在 storage 中
6. **实际**：_____

## 已验证的改动

✅ content-script.js：
- `getPageMode()` 函数：现在同时检查 pathname 和 tab 参数，防止误报
- `autoTriggerDownload()` 函数：商品分析模式下新增前置动作，点击"内容消费"tab
- 下载超时：20秒 → 120秒（2分钟）
- `buildPanelHTML()` 函数：作品分析模式新增商品ID列，带天猫链接
- 新增 CSS：`.col-product` 和 `.product-link` 样式

## 代码变更摘要

| 改动 | 文件 | 行数 | 说明 |
|-----|------|------|------|
| 页面检测收紧 | content-script.js | 14-19 | 检查 pathname 和 tab 参数 |
| 商品分析前置动作 | content-script.js | 500-511 | 点击"内容消费"tab |
| 超时扩展 | content-script.js | 557 | 20000 → 120000 ms |
| 商品ID列表头 | content-script.js | 270-271 | 作品分析模式新增表头 |
| 商品ID列内容 | content-script.js | 282-299 | 商品ID + 天猫链接 |
| CSS 样式 | content-script.js | 679-680 | 新增 product-link 样式 |

未改动文件：`manifest.json`, `page-hook.js`, `background.js`, `rules.js`, `vendor/xlsx.full.min.js`

## 如发现问题

### 调试步骤

1. 打开 Chrome DevTools (`F12`) → 切换到对应的光合平台标签页
2. 查看 `Console` 标签，搜索 `[光合分析]` 的日志
3. 检查 `Application` → `Storage` → `Local storage` 中的两个 key：
   - `gh_last_results`：作品分析数据
   - `gh_product_results`：商品分析数据
4. 记录控制台中的错误信息

### 常见问题排查

**问题：按钮不显示**
- 确认 URL 包含 `tab=singleEffect` 或 `tab=productAnalysis`
- 确认 URL 路径包含 `/page/unify/asset-overview`
- 检查 Console 中是否有错误信息
- 尝试刷新插件（Chrome 扩展页面的刷新按钮）

**问题：点击"重新获取"后提示超时**
- 确认网络连接正常
- 检查是否已关闭下载提示框（允许下载）
- 大文件可能需要等待超过 20 秒，现在超时时间是 120 秒
- 如仍超时，尝试手动下载后点击"上传 Excel"

**问题：商品分析页数据为空或错误**
- 确认自动点击了"内容消费"tab（检查进度条消息）
- 确认下载的 Excel 包含必需的字段（查看次数、有效查看次数等）
- 尝试手动上传 Excel 文件测试

**问题：商品ID列没有链接**
- 确认当前在作品分析页面（`tab=singleEffect`）
- 确认 Excel 中有 `商品ID` 或 `商品id` 字段
- 刷新插件后重试
